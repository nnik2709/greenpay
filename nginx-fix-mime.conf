# Nginx configuration specifically for fixing MIME type issues
# This configuration ensures JavaScript modules are served with correct MIME types

server {
    listen 80;
    listen [::]:80;
    server_name eywademo.cloud www.eywademo.cloud 195.200.14.62;
    
    root /var/www/png-green-fees/dist;
    index index.html;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline' 'unsafe-eval'" always;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
    
    # Handle client-side routing (but not for assets)
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Specific handling for the problematic JavaScript file
    location ~* index-[a-f0-9]+\.js$ {
        add_header Content-Type "application/javascript; charset=utf-8";
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
        add_header Access-Control-Allow-Headers "Content-Type";
        
        # Ensure this is treated as JavaScript, not HTML
        try_files $uri =404;
    }
    
    # All JavaScript files
    location ~* \.js$ {
        add_header Content-Type "application/javascript; charset=utf-8";
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
        
        # Ensure this is treated as JavaScript, not HTML
        try_files $uri =404;
    }
    
    # CSS files
    location ~* \.css$ {
        add_header Content-Type "text/css; charset=utf-8";
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
    
    # JSON files
    location ~* \.json$ {
        add_header Content-Type "application/json; charset=utf-8";
        expires 1d;
        try_files $uri =404;
    }
    
    # Images
    location ~* \.(png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
    
    # Fonts
    location ~* \.(woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
    
    # Security - deny access to hidden files
    location ~ /\. {
        deny all;
    }
    
    # Security - deny access to backup files
    location ~ ~$ {
        deny all;
    }
}
