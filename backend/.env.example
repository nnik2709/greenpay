# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=greenpay
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT Configuration
JWT_SECRET=your_jwt_secret_here_make_it_long_and_random

# Server Configuration
PORT=3001
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# SMTP Email Configuration (for invoice emailing)
# Gmail example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=PNG Green Fees System

# For Gmail: Use App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords

# Other SMTP providers:
# - Office 365: smtp.office365.com, port 587
# - Outlook: smtp-mail.outlook.com, port 587
# - Yahoo: smtp.mail.yahoo.com, port 587
# - Custom SMTP: Configure host, port, and credentials

# ============================================
# PAYMENT GATEWAY CONFIGURATION
# ============================================
# Choose which gateway to use: stripe | bsp | kina
# For testing/POC: use 'stripe'
# For production: use 'bsp' or 'kina'
PAYMENT_GATEWAY=stripe

# ============================================
# STRIPE PAYMENT GATEWAY (For Testing/POC)
# ============================================
# Get your API keys from: https://dashboard.stripe.com/test/apikeys
# Use TEST keys only (sk_test_... and pk_test_...)
STRIPE_SECRET_KEY=sk_test_YOUR_TEST_SECRET_KEY_HERE
STRIPE_WEBHOOK_SECRET=whsec_test_YOUR_WEBHOOK_SECRET_HERE

# WARNING: Only test keys allowed during POC phase!
# Production keys (sk_live_...) will be rejected

# ============================================
# BSP BANK PNG (Production Gateway)
# ============================================
# Contact BSP: servicebsp@bsp.com.pg | +675 3201212
# Uncomment and configure when ready for production

# BSP_MERCHANT_ID=your_bsp_merchant_id
# BSP_API_KEY=your_bsp_api_key
# BSP_WEBHOOK_SECRET=your_bsp_webhook_secret
# BSP_MODE=sandbox
# # BSP_MODE=production  # Use this when going live
# BSP_SANDBOX_URL=https://sandbox-bsp.example.com
# BSP_PRODUCTION_URL=https://api-bsp.com.pg

# ============================================
# KINA BANK (Alternative Production Gateway)
# ============================================
# Uncomment if using Kina Bank instead of BSP

# KINA_MERCHANT_ID=your_kina_merchant_id
# KINA_API_KEY=your_kina_api_key
# KINA_WEBHOOK_SECRET=your_kina_webhook_secret
# KINA_MODE=sandbox
# # KINA_MODE=production  # Use this when going live
# KINA_SANDBOX_URL=https://sandbox-kina.example.com
# KINA_PRODUCTION_URL=https://api-kina.com.pg
