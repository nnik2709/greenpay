# Backend API Configuration
# For local development with remote backend:
VITE_API_URL=https://greenpay.eywademo.cloud/api

# For local development with local backend:
# VITE_API_URL=http://localhost:3001/api

# Supabase Configuration (Legacy - migrated to PostgreSQL)
# Get these values from your Supabase project dashboard
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Admin Email Configuration
# Email address to receive admin notifications (ticket creation, new users, etc.)
VITE_ADMIN_EMAIL=admin@yourdomain.com

# Stripe Payment Gateway (for testing/POC)
# Get from: https://dashboard.stripe.com/test/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_PUBLISHABLE_KEY_HERE

# Payment Gateway Configuration (Optional - can be configured via admin UI)
# Kina Bank Internet Payment Gateway
# VITE_KINA_BANK_MERCHANT_ID=your_merchant_id
# VITE_KINA_BANK_API_ENDPOINT=https://api.kinabank.com.pg/payment
# VITE_KINA_BANK_SANDBOX_MODE=true

# ============================================
# BSP DOKU PAYMENT GATEWAY (Backend)
# ============================================
# BSP Bank PNG - DOKU Hosted Payment Pages Integration
# API Version: 1.29
# Contact: servicebsp@bsp.com.pg | +675 3201212
#
# SECURITY: NEVER commit actual credentials to git!
# Add these to production .env file only
#
# Test Environment Credentials (provided by BSP):
BSP_DOKU_MALL_ID=11170
BSP_DOKU_SHARED_KEY=ywSd48uOfypN
BSP_DOKU_MODE=test
BSP_DOKU_CHAIN_MERCHANT=NA
#
# Production Credentials (request from BSP for production):
# BSP_DOKU_MALL_ID=your_production_mall_id
# BSP_DOKU_SHARED_KEY=your_production_shared_key
# BSP_DOKU_MODE=production
# BSP_DOKU_CHAIN_MERCHANT=NA
#
# Optional Settings:
# BSP_DOKU_TIMEOUT=30000  # Request timeout in milliseconds (default: 30000)
# BSP_DOKU_MAX_RETRIES=3  # Max retry attempts (default: 3)
#
# Currency Testing (ISO 3166 numeric codes):
# BSP_DOKU_TEST_CURRENCY=360  # IDR (Indonesian Rupiah) - for testing DOKU integration
# BSP_DOKU_TEST_CURRENCY=598  # PGK (Papua New Guinea Kina) - production default
# Default: 598 (PGK) if not specified
#
# Webhook URLs (provide to BSP for configuration):
# Notify URL: https://greenpay.eywademo.cloud/api/payment/webhook/doku/notify
# Redirect URL: https://greenpay.eywademo.cloud/api/payment/webhook/doku/redirect
#
# IP Whitelisting (configure in firewall):
# Test IPs: 103.10.130.75, 147.139.130.145
# Production IPs: 103.10.130.35, 147.139.129.160
# ============================================

# ============================================
# EMAIL SERVICE SETUP (Supabase Edge Functions)
# ============================================
#
# The application uses Supabase Edge Functions for sending emails.
# Configure these in your Supabase Dashboard under Edge Functions settings:
#
# Required Edge Function Environment Variables:
# - RESEND_API_KEY: Your Resend API key (get from https://resend.com)
# - FROM_EMAIL: Verified sender email (e.g., "PNG Green Fees <noreply@yourdomain.com>")
#
# Supabase Auth Email Configuration:
# - Go to Supabase Dashboard > Authentication > Email Auth
# - For production: Configure custom SMTP or use Supabase's default
# - Set your sender email address
# - Customize email templates if needed
#
# For detailed setup instructions, see:
# - SUPABASE_SETUP.md
# - TEST_EMAIL_FUNCTIONALITY.md
# ============================================
